spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: test-secret-key-that-is-at-least-256-bits-long-for-hmac-sha256-testing-only
    expiration-ms: 3600000
  admin:
    email: admin@collabnex.com
    password: changeme
  storage:
    upload-dir: target/test-uploads
